# 跨平台共享绑定占位

本目录当前是显式占位目录，不包含可编译绑定代码，也没有被 Android 或 Linux 构建链路引用。

保留原因：后续如果确实出现跨平台共享绑定代码，可在这里落地；在此之前，平台绑定分别维护在 `bindings/android` 和各客户端自己的后端适配层中。

当前文件：

| 文件 | 用途 |
|------|------|
| `.gitkeep` | 保持占位目录被 Git 跟踪 |
| `README.md` | 说明该目录目前没有运行期职责 |
