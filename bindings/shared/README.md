# 跨平台共享绑定

本目录用于存放跨平台共享的绑定代码，目前尚未有具体内容。

## 主要文件

| 文件 | 用途 |
|------|------|
| `.gitkeep` | 占位文件，保持目录在 Git 中存在 |

## 规划用途

- 存放 Android 和 Linux 客户端共用的绑定代码
- 统一跨平台的 API 调用接口
- 减少重复的桥接代码

## 依赖关系

- 将依赖 `core/writer_core` Rust 核心库
- 被 `bindings/android` 和 `bindings/linux` 共同使用

## 使用说明

- 目前为空目录，未来将逐步完善跨平台绑定