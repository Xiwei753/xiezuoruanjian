[package]
name = "writer_core_jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = []
ai = ["writer_core/ai"]

[dependencies]
jni = "0.21.1"
writer_core = { path = "../../core/writer_core" }
serde_json = "1.0"
serde = { version = "1.0.228", features = ["derive"] }
