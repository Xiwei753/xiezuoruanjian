# Android Bindings (JNI)

This directory is reserved for the Rust JNI wrappers that expose `writer_core`'s Facade to the `apps/android` Kotlin client.

*TODO: Implement `uniffi` or raw JNI methods to map `facade.rs`.*
