[package]
name = "writer_platform_api"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.8"
serde_json = "1.0"
