NDK_VERSION=r25c
_NDK_ENV_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
NDK_VERSION_NUMERIC=$(grep 'sujian.android.ndkVersion' "$_NDK_ENV_DIR/../../apps/android/gradle.properties" | cut -d= -f2 | tr -d '[:space:]')
unset _NDK_ENV_DIR
