<?xml version="1.0" encoding="UTF-8"?>
<issues format="6" by="lint 8.9.2" type="baseline" client="gradle" dependencies="false" name="AGP (8.9.2)" variant="all" version="8.9.2">

    <!-- baseline 保留理由（Issue #597 lint baseline 清理评估结论）：
         这 3 条 NewApi 全部来自 UniFFI 生成代码 writer_core.kt 的
         java.lang.ref.Cleaner（API 33+，minSdk 24）。

         为什么不能删除 baseline / 不能让 lint 不分析 generated 源码：
         1. AGENTS.md 禁止手工修改生成绑定；生成结果有问题时修上游契约。
            Cleaner 的根因在 UniFFI bindgen 上游（生成代码用了 API 33+ 的
            Cleaner），应修上游 UniFFI 契约或升级 UniFFI 版本，不在本仓库范围。
         2. AGP 8.9.2 的 lint task（AndroidLintTask/AndroidLintAnalysisTask）
            继承自 NonIncrementalTask，不是 Gradle SourceTask，没有 exclude()
            方法。无法像 ktlint/detekt 那样在 task 上 exclude("**/generated/**")。
         3. AGP lint DSL 的 checkGeneratedSources（默认 false）只排除 AGP 内部
            标记为 generated 的源码（R/BuildConfig/AIDL），不排除手动加入
            kotlin.srcDirs 的 generated/writer-uniffi。Variant API 的
            addGeneratedSourceDirectory 需要关联生成源码的 Gradle task，但
            kotlin 绑定由外部脚本 tools/build_android.sh（cargo run
            uniffi-bindgen）生成，非 Gradle task，无法直接注册。
         4. disable += "NewApi" 会全局禁用，隐藏所有真实 NewApi，违反 AGENTS.md
            "不隐藏新增真实问题"。不采用。

         baseline 是 AGP 官方推荐的"冻结预存问题"机制：精确冻结这 3 条
         （file/line/column/message），新增 NewApi 仍会报，不隐藏新增真实问题。
         这是当前架构下最干净、符合 AGENTS.md 的解法。

         TODO（上游）：修 UniFFI bindgen 契约让生成代码不用 java.lang.ref.Cleaner
         （或升级 UniFFI 版本至生成代码兼容 minSdk 24 的版本），届时这 3 条
         NewApi 消失，baseline 可删除。

         lint 只检查 noAiDebug（见 app/build.gradle.kts lint 块），ai 变体的
         location 已移除——ai 路径在 noAiDebug 检查中不会出现，保留即失效记录。 -->
    <issue
        id="NewApi"
        message="Call requires API level 33 (current min is 24): `java.lang.ref.Cleaner#create`"
        errorLine1="    val cleaner = java.lang.ref.Cleaner.create()"
        errorLine2="                                        ~~~~~~">
        <location
            file="build/generated/writer-uniffi/noAi/kotlin/uniffi/writer_core/writer_core.kt"
            line="2897"
            column="41"/>
    </issue>

    <issue
        id="NewApi"
        message="Call requires API level 33 (current min is 24): `java.lang.ref.Cleaner#register`"
        errorLine1="        JavaLangRefCleanable(cleaner.register(value, cleanUpTask))"
        errorLine2="                                     ~~~~~~~~">
        <location
            file="build/generated/writer-uniffi/noAi/kotlin/uniffi/writer_core/writer_core.kt"
            line="2900"
            column="38"/>
    </issue>

    <issue
        id="NewApi"
        message="Call requires API level 33 (current min is 24): `java.lang.ref.Cleaner.Cleanable#clean`"
        errorLine1="    override fun clean() = cleanable.clean()"
        errorLine2="                                     ~~~~~">
        <location
            file="build/generated/writer-uniffi/noAi/kotlin/uniffi/writer_core/writer_core.kt"
            line="2906"
            column="38"/>
    </issue>

</issues>
