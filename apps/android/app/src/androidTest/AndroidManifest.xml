<?xml version="1.0" encoding="utf-8"?>
<!--
  androidTest 源集专属 Manifest（只打进 test APK com.xiwei.sujian.test，绝不进入生产 APK）。

  #641：旧确定性测试 IME（TestInputMethodService / TestImeCommandService）只服务
  已删除的 EditorImeInputTest（旧 SujianEditorView + InputConnection + AndroidInputAdapter
  路径）。正文输入边界已改为 state-based BasicTextField(TextFieldState)，instrumented
  测试通过 Compose semantics（testTag = editor.content）驱动，不再需要 test IME service。
  对应的 TestImeCommands / TestInputMethodService / TestImeCommandService / res/xml/method
  已一并删除。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application />

</manifest>
