<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Writing app: do NOT backup manuscripts, sync tokens, logs, or crash files.
         Only allow non-sensitive device settings if backup is ever re-enabled.
         Currently allowBackup=false, so this file is a placeholder for future use. -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
