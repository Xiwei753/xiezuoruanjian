<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_editor.xml — EditorActivity 的布局（SinglePane 手机模式）

    包含独立的 toolbar（CoordinatorLayout + AppBarLayout）和 EditorFragment 容器。
    EditorFragment 的核心编辑器布局在 fragment_editor.xml 中定义。
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/editorCoordinatorLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.EditorActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/ic_back"
            app:subtitle="@string/status_saved" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- EditorFragment 容器 -->
    <FrameLayout
        android:id="@+id/editorFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
