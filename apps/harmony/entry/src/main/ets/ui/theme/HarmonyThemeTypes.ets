// =============================================================================
// HarmonyThemeTypes.ets — 主题类型定义（打破双向 import 环）
// =============================================================================
//
// Issue #767 评论 5831658277：HarmonyThemeAdapter 和 HarmonyColorModeController
// 互相 import 形成循环依赖。把 ThemeMode 类型提取到独立文件，让两者都从这里
// import，消除环。
// =============================================================================

export type ThemeMode = 'light' | 'dark' | 'system'
