🎯 What:
修复了 `writer_core_ensure_device_info` 同步操作中，`platform` 和 `device_class` 字符串在解析后未进行验证的漏洞。现在会在转换为 Rust 字符串后，立即对这俩参数进行长度（<=64）以及字符（仅包含字母数字、下划线或连字符）的安全校验。

⚠️ Risk:
如果未经任何验证直接处理平台或设备信息字符串并作为数据持久化，攻击者可能通过构造超长字符串尝试引发 DoS 问题，或注入恶意字符尝试攻击后续将此数据落盘或查询使用的依赖组件，扩大爆炸半径。

🛡️ Solution:
在 `c_str_to_rust` 解析完成后增加基于 `len` 与 `chars().all` 的格式检测拦截机制。如果输入不合法（过长或包含非法字符），则直接通过原有的标准 JSON 错误格式 `err_json` 返回错误，从而安全封闭入口并保证逻辑健壮性。
