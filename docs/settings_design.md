# 设置系统设计文档 (Settings Design)

本系统旨在提供一个长期可扩展的配置管理架构，用于本地优先、注重隐私的写作应用。

## 核心架构分类

系统强制将所有设置区分为两大类：

### 1. `LocalSettings` (本机设置，不同步)
- **用途**: 保存仅与当前物理设备相关的状态。
- **存储位置**: 通过 `path_provider` 保存在操作系统的应用支持目录中 (`settings.local.json`)，完全脱离用户的项目工作区。
- **典型字段**:
  - `workspacePath`: 工作区绝对路径 (因不同设备路径不同，严格不能同步)。
  - `deviceName`: 设备标识。
  - `windowWidth` / `windowHeight`: 窗口尺寸。

### 2. `SyncableSettings` (可同步设置)
- **用途**: 保存用户针对整个私人文稿库的统一偏好和配置。
- **存储位置**: 保存在工作区内的元数据目录中 (`<workspacePath>/app-meta/settings/settings.sync.json`)。
- **典型字段**:
  - 编辑器偏好 (字号、主题、行高)。
  - 自动保存与备份策略。
  - AI 提供商配置与模型选择。
  - GitHub 同步配置。

## 密钥明文存储的设计决策

**当前设计允许 API Key (如 DeepSeek) 和同步 Token (如 GitHub Token) 以明文形式存储在 `settings.sync.json` 中。**

### 原因与上下文
这是一个有意识的用户需求折衷设计：
- 本软件的核心理念是**本地优先**，所有用户数据(包括配置)最终都会推送到用户自己配置的 **私人 GitHub 仓库** (非软件源码仓库) 作为备份与多端同步节点。
- 由于是私人仓库，用户希望能够在多设备之间无缝同步这些 API 密钥，而无需在每台新设备上重新手动填写复杂的 Token。
- 当前 MVP 阶段未引入复杂的非对称加密同步方案，因此选择了最直接的明文 JSON 存储。

### 风险提示
- 密钥明文保存在本地磁盘上。
- **Git 历史保留**: 即使未来更改了密钥，旧的密钥依然会存在于私人文稿仓库的 Git 提交历史中。
- **泄露风险**: 若用户的私人文稿仓库权限配置错误(变成 Public)或私人电脑被攻破，密钥存在泄露风险。

### UI 缓解措施
为了确保用户的知情权，UI 设计上采取了以下弱干扰但明确的提醒：
1. UI 输入框默认使用掩码 (`obscureText`) 隐藏密钥，提供显示/隐藏按钮。
2. 静态免责声明：在每个密钥输入框下方，直接附带一段不可忽略的提示文字：
   > “此密钥会随私人文稿仓库同步保存。请确认该仓库为私有仓库，并理解 Git 历史可能长期保留已提交的密钥。如密钥泄露，请在对应服务商后台重置。”
3. 决不使用强制弹窗阻断用户的保存操作。

## 错误恢复与容灾
- **JSON 损坏处理**: `SettingsService` 在读取 JSON 文件时如果捕获到 `FormatException`，不会引发 App 崩溃，而是会安全回退到默认的设置对象。
- **原子写入**: `SyncableSettings` 的修改必须通过 `AtomicWriter` 排队写入，借助操作系统的原子重命名机制，防止写入中断导致配置文件清零。
