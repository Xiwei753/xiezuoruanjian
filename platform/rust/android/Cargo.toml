[package]
name = "writer-platform-android"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
writer_core = { path = "../../../core/writer_core", features = ["github-api"] }
writer_platform_api = { path = "../../../core/writer_platform_api" }
writer_uniffi = { path = "../../../core/writer_uniffi" }
