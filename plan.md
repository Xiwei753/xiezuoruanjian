1. **Explore `writer_core` for test gaps.**
   - We need to add tests for low-risk, stable core logic.
   - Identified DTO serialization/deserialization as a stable area.
   - We successfully added several tests for DTO contracts. We will submit these tests.
2. **Review the PR.**
   - We must review our code.
   - Our PR description and title must follow the exact rules: `🧪 [<description>]` for tests, and sections `🎯 What`, `📊 Coverage`, and `✨ Result`.
3. **Complete pre-commit steps.**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
4. **Submit PR.**
   - Create PR using `submit` tool.
