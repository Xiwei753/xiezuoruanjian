1. Verify the current codebase layout meets the user request.
- Ensure `core/writer_core` contains the Rust implementation and passes tests.
- Ensure `apps/flutter_legacy` exists and contains the old flutter app.
- Ensure `apps/android_native` and `apps/linux_native` have minimal skeleton files.
- Ensure all docs exist and match specifications.
- Ensure `fixtures/sample_workspace` and `tools/validate_workspace.py` exist and work.
- Ensure `tools/check_all.sh` exists and successfully checks everything.
- Ensure github workflows are added.
2. Complete Pre-commit steps.
3. Submit the changes.
